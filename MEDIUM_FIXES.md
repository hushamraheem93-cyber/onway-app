# MEDIUM_FIXES.md — إغلاق البندين المتوسطين المُعطِّلين للإنتاج

**المرجع:** `FINAL_SADD_AUDIT.md` · `HIGH_FIXES.md`
**النطاق:** **M6 و M8 فقط**. لم يُلمس أي بند Medium آخر ولا أي بند Low، ولم يُمَسّ أي إصلاح Critical أو High.
**التاريخ:** 2026-07-26
**الفرع:** `claude/onway-enterprise-review-q1d18o`

---

## ملخّص

| البند | الحالة | الملف المعدَّل |
|-------|--------|----------------|
| M6 — عدم تطابق فهرس كوبونات الخصم | ✅ مُغلق | `firestore.indexes.json` |
| M8 — فهرس `settlementPayments` المفقود | ✅ مُغلق | `firestore.indexes.json` |

**حجم الرقعة في كود الإنتاج:** ملف واحد، **+10 / −1 سطر**. لم يُعدَّل أي ملف `.ts` أو `.tsx`.

| البوابة | النتيجة |
|---------|---------|
| Typecheck | ✅ نظيف |
| Tests | ✅ **160 ناجح / 0 فاشل** (39 مجموعة) — كانت 151 |
| Build | ✅ `server_dist/index.js` — 531.7 kb |
| Mutation | ✅ **5/5** طفرات تُكتشف |
| JSON | ✅ `firestore.indexes.json` يُحلَّل بنجاح |

---

## M6 — فهرس الكوبونات يُسمّي مجموعة لا يستخدمها الكود

### التحقّق من استمرار الخلل
نعم، ما زال قائماً. الفهرس كان مُعرَّفاً على `promoUsage`:
```json
{ "collectionGroup": "promoUsage",
  "fields": [ { "fieldPath": "userId" }, { "fieldPath": "promoCode" } ] }
```
بينما كل استعلام في الكود يقصد `promoUsageHistory`:
- `server/firebase.ts:1461` — `checkPromoUsage()`: `.where("userId","==").where("promoCode","==").limit(1)`
- `server/firebase.ts:1475` — `recordPromoUsage()`: كتابة
- `server/routes.ts:2095` — فحص `maxUsage`: `.where("promoCode","==")`

بحث نصّي على كامل الخادم: **صفر** تطابقات لـ `.collection("promoUsage")`.

### السبب الجذري
اسم المجموعة في تعريف الفهرس خاطئ. Firestore لا يشتكي من فهرس لمجموعة غير موجودة — يُنشئه بصمت ولا يُستخدم أبداً. النتيجة أن الاستعلام الحقيقي **ذو الحقلين** في `checkPromoUsage()` يبقى بلا فهرس مركّب، فيرمي `FAILED_PRECONDITION`، و`catch` الخاصّة به تُرجع `false` — أي **"هذا المستخدم لم يستخدم الكوبون"** — فيصير الكوبون أحادي الاستخدام قابلاً لإعادة الاستخدام بلا حدّ.

هذا خلل **في إصلاحي السابق**: أضفت الفهرس ظنّاً منّي أنني غطّيت الاستعلام، فبدا الأمر مُنجَزاً بينما لم يكن يحمي شيئاً — والأسوأ أن وجوده أخفى المشكلة.

### التغيير
```diff
-      "collectionGroup": "promoUsage",
+      "collectionGroup": "promoUsageHistory",
```
لا تغيير في الحقول ولا في ترتيبها — هما صحيحان أصلاً ويطابقان `checkPromoUsage()` تماماً.

الاستعلام الثاني (`routes.ts:2095`) يرشّح على `promoCode` وحده، وهو حقل مفرد تفهرسه Firestore تلقائياً، فلا يحتاج تعريفاً.

### لماذا الإصلاح آمن
- **لا تغيير في مخطّط البيانات**: لم تُنشأ مجموعة ولا يُعاد تسمية شيء؛ `promoUsageHistory` هي المجموعة القائمة منذ البداية.
- **لا تغيير في الكود**: لم يُلمس أي ملف مصدر.
- الفهرس القديم على `promoUsage` كان خاملاً بالكامل، فحذفه لا يمكن أن يكسر استعلاماً.

---

## M8 — فهرس `settlementPayments` المطلوب لإصلاح FIFO مفقود

### التحقّق من استمرار الخلل
نعم. `repairPendingFIFO()` في `server/settlement.ts:613` تُنفّذ:
```ts
await db.collection(SETTLEMENT_PAYMENTS)
  .where("accountKey", "==", key)
  .where("fifoApplied", "==", false)
  .orderBy("createdAt", "desc")
  .limit(20)
  .get();
```
مساواتان + `orderBy` على حقل ثالث ⇒ يتطلّب فهرساً مركّباً. لم يكن في `firestore.indexes.json` **أي** مدخل لـ `settlementPayments`.

### السبب الجذري
الفهرس المركّب مفقود، فالاستعلام يرمي `FAILED_PRECONDITION` في **كل** استدعاء. والدالة مُحاطة بـ `try/catch` عمداً ("الإصلاح جهد أفضل — لا يجوز أن يُعطّل المستدعي")، فالاستثناء يُسجَّل ويُبتلع. النتيجة: **مرور الإصلاح لم يعمل ولا مرّة واحدة**.

`completeSettlement` تستدعيه في بدايتها تحديداً لمعالجة الحالة التي تنهار فيها العملية بين إتمام معاملة الدفتر وخطوة محاسبة FIFO. عند حدوث ذلك تبقى الدفعة `fifoApplied: false` **إلى الأبد**: الرصيد الإجمالي صحيح، لكن سجلّات التسوية لكل طلب على حدة لا تُغلق أبداً، فكشف الحساب يعرض طلبات "غير مسوّاة" رغم أن مالها قُبض.

### التغيير
```json
{
  "collectionGroup": "settlementPayments",
  "queryScope": "COLLECTION",
  "fields": [
    { "fieldPath": "accountKey",  "order": "ASCENDING"  },
    { "fieldPath": "fifoApplied", "order": "ASCENDING"  },
    { "fieldPath": "createdAt",   "order": "DESCENDING" }
  ]
}
```
حقلا المساواة أولاً، ثم حقل الترتيب بالاتجاه المطابق للاستعلام (`desc`). فهرس بـ `createdAt` تصاعدي **لا يُلبّي** استعلاماً تنازلياً — لذلك يوجد اختبار مستقل للاتجاه وحده.

### لماذا الإصلاح آمن
- **إضافة بحتة**: لا تعديل ولا حذف لأي فهرس قائم.
- `getSettlementPayments()` (`settlement.ts:847`) يرشّح على `accountKey` وحده — حقل مفرد، غير متأثّر.
- **لم أُعدّل `settlement.ts` إطلاقاً.** كان بإمكاني جعل الفشل صاخباً بتمييز `FAILED_PRECONDITION` عن الأخطاء العابرة، لكن السبب الجذري هو الفهرس المفقود، وسلوك "جهد أفضل لا يُعطّل الدفع" مقصود وصحيح — تغييره سيكون توسيعاً للنطاق.

---

## التحقّق المُنفَّذ

### اختبارات الانحدار — ملف جديد `tests/unit/firestore-indexes.test.mjs` (9 اختبارات)

هذا الصنف من الأخطاء **غير مرئي تماماً وقت التشغيل**: الفهرس المفقود لا يكسر البناء ولا يُفشل الـ typecheck، وفي حالة M8 لا يصل حتى إلى المستخدم. الشيء الوحيد الذي يلتقطه هو فحص يقرأ الاستعلامات من المصدر ويواجهها بملف الفهارس — وهذا ما يفعله الملف:

1. **يثبت الفرضية أولاً** — أن الكود يستعلم `promoUsageHistory` ولا يمسّ `promoUsage`. لو أُعيدت تسمية المجموعة يوماً، يفشل هذا الاختبار **أوّلاً** ويشير إلى السبب الحقيقي بدل اتّهام الفهرس.
2. **يثبت شكل استعلام FIFO** كما هو مكتوب في `settlement.ts` قبل التحقّق من الفهرس المقابل — فلا يمكن أن يبقى الفهرس "صحيحاً" بينما الاستعلام تغيّر تحته.
3. **يحرس الصنف كلّه**: كل `collectionGroup` في ملف الفهارس يجب أن يكون مجموعة يلمسها الكود فعلاً (بما فيها تلك التي تُستدعى عبر ثوابت مثل `SETTLEMENT_PAYMENTS`). هذا بالضبط ما كان سيلتقط M6 يوم إنشائه.
4. يتحقّق أن الملف هو الذي يشير إليه `firebase.json` — أي أن ما نُحرّره هو ما ينشره `firebase deploy` فعلاً.
5. يرفض الفهارس أحادية الحقل (تُنشئها Firestore تلقائياً؛ تعريف واحد منها إشارة إلى أن الكاتب أخطأ في تحديد الاستعلام المطلوب تغطيته).

### اختبار الطفرات — 5/5

| الطفرة | النتيجة | الاختبارات التي فشلت |
|--------|---------|----------------------|
| إعادة تسمية الفهرس إلى `promoUsage` | ✅ أحمر | 3 |
| حذف فهرس الكوبونات كلياً | ✅ أحمر | 1 |
| حذف فهرس `settlementPayments` | ✅ أحمر | 2 |
| جعل `createdAt` تصاعدياً | ✅ أحمر | 2 |
| إسقاط `fifoApplied` من الفهرس | ✅ أحمر | 1 |

بعد الاسترجاع: 9/9 ناجح.

### البوابات
- `npx tsc --noEmit` — نظيف (عدا خطأَي `expo-file-system` القائمين قبل كل هذا العمل).
- `npm run test:unit` — **160/160**، ولا تراجع في أي من مجموعات Critical أو High.
- `npm run server:build` — ناجح.
- `npx eslint tests/unit/firestore-indexes.test.mjs` — نظيف.

### ما لم يُتحقَّق منه
لا يمكن التحقّق من بناء الفهرس فعلياً في هذه البيئة: لا توجد بيانات اعتماد Firebase ولا Firebase CLI موثَّقة. **الفهارس لا تصبح فعّالة إلا بنشرها**:

```bash
firebase deploy --only firestore:indexes
```

هذه الخطوة مذكورة في `DEPLOYMENT_CHECKLIST.md:72` ولم تُنفَّذ بعد. حتى تُنفَّذ، يبقى كلا الخللين **قائمين في الإنتاج** رغم صحّة الملف — ملف الفهارس هو تعريف، لا حالة.

---

## بنود Medium المتبقّية (17 — لم تُلمس)

| # | البند | الملف |
|---|-------|-------|
| M1 | `assignWaitingBatchToDriver` بلا قفل — استدعاءات متزامنة تُنشئ دفعات مكرّرة لنفس الطلبات | `routes.ts` |
| M2 | `report-issue` يتجاوز آلة الحالة → `delivered → issue → cancelled` على طلبات مسوّاة، بلا عكس للقيد | `routes.ts` |
| M3 | `adminAdjustLedger` يقبل `NaN` ويكتبه في الدفتر نهائياً | `settlement.ts` |
| M4 | إسناد الطلب قابل للانتحال — `POST /api/orders` لا يقارن `phoneNumber` بالهاتف الموثَّق | `routes.ts` |
| M5 | استرداد الكوبون check-then-act بلا مُعرِّف حتمي → كوبون أحادي الاستخدام يُستَرد مرّتين | `routes.ts` |
| M7 | فهرس مفقود — تحليلات التاجر (`vendorId + status + orderBy createdAt`) | `vendor.ts:2097` |
| M9 | فهرس مفقود — مرشِّح التقييمات "مع صور" | `routes.ts` |
| M10 | مصفوفة رسائل `supportChats`: قراءة-تعديل-كتابة غير ذرّية، بلا سقف | `firebase.ts` |
| M11 | التاجر يستطيع كتابة تقييم متجره بنفسه، بلا حدّ | `vendor.ts:415` |
| M12 | تبويب إدارة المحتوى في اللوحة لا يفعل شيئاً ويُبلّغ بالنجاح | `admin.html` |
| M13 | `confirmDelete` بلا فرع `else` على `response.ok` | `admin.html` |
| M14 | استطلاع إشعارات التاجر يُصفّر `isFirstLoad` عند كل تغيّر AppState | `VendorNotificationsContext.tsx` |
| M15 | `uncaughtException` يُسجَّل ويُبتلع — يُعطّل تعافي PM2 | `index.ts` |
| M16 | N+1 عند إنشاء الطلب — نفس مستندات `vendorProducts` تُجلب مرّتين | `routes.ts` |
| M17 | `/api/admin/dashboard-stats` يُحمّل 5 مجموعات كاملة في الذاكرة | `routes.ts` |
| M18 | `nginx.conf` لا يُحمَّل كما هو موثَّق — `listen 443 ssl` مع كل `ssl_certificate` مُعلَّقة | `deployment/nginx.conf` |
| M19 | `esbuild`/`compression`/`jsonwebtoken` مُستخدَمة في الإنتاج وغير معرَّفة في أي كتلة اعتماديات | `package.json` |

> **ملاحظتان تستحقّان الانتباه ضمن ما تبقّى:** `M7` و`M9` من نفس صنف M8 تماماً (فهارس مركّبة مفقودة)، وستفشلان بـ `FAILED_PRECONDITION` عند أول استخدام إنتاجي — تحليلات التاجر وصفحة تقييمات المتجر. لم أُصلحهما لأنك حدّدت M6 و M8 حصراً؛ اختبار الفهارس الجديد يُغطّي البنية التي تجعل إصلاحهما لاحقاً سطراً واحداً لكل منهما. و`M19` يعني أن بناء إنتاج نظيف قد يفشل إن توقّف أحد الاعتماديات غير المباشرة عن جلبها.

## بنود Low المتبقّية (6 — لم تُلمس)

| # | البند | الملف |
|---|-------|-------|
| L1 | مؤقّت/سوكِت GPS للسائق قد يستمرّ بعد "عدم الاتصال" | `DriverHomeScreen.tsx` |
| L2 | `OrderTrackingScreen` بحالة تحميل لا نهائية بلا إعادة محاولة | `OrderTrackingScreen.tsx` |
| L3 | `OrderConfirmationScreen` بحالة تحميل بلا رجوع أو إعادة محاولة | `OrderConfirmationScreen.tsx` |
| L4 | معالجات حفظ في لوحتَي التاجر والأدمن تُظهر نجاحاً عند الفشل | `VendorOrdersScreen.tsx` · `admin.html` · `AdminScreen.tsx` |
| L5 | `uncaughtException` يُبتلع (نسخة L من M15) | `index.ts` |
| L6 | `res.status(500).json({error: error.message})` في ~20 معالجاً | `routes.ts` |

> **L6 أُغلق فعلياً** كأثر جانبي لـ H23: صفر مواضع `error.message` متبقّية في أي استجابة 5xx عبر `routes.ts` و`vendor.ts` و`index.ts`. أُبقيه مدرَجاً هنا للأمانة مع التدقيق الأصلي، لا لأن العمل عليه ما زال مطلوباً.

---

# Production blockers remaining: **YES**

**مُعطِّل واحد، وهو تشغيلي لا برمجي:**

**الفهارس لم تُنشر.** `firestore.indexes.json` صار صحيحاً ومُختبَراً، لكن ملف الفهارس تعريف لا حالة. حتى تُنفَّذ `firebase deploy --only firestore:indexes`، يبقى الخللان اللذان أُغلقا للتوّ **قائمين حرفياً في الإنتاج**: `checkPromoUsage()` ستستمرّ في إرجاع `false` فيبقى الكوبون أحادي الاستخدام قابلاً للتكرار، و`repairPendingFIFO()` ستستمرّ في الفشل الصامت. هذه الخطوة بيدك ولا أستطيع تنفيذها هنا (لا بيانات اعتماد Firebase في هذه البيئة).

**وبنداً Medium من نفس الصنف يبقيان قائمين بقرارك:** `M7` (تحليلات التاجر) و`M9` (تقييمات المتجر مع صور) سيفشلان بـ `FAILED_PRECONDITION` عند أول استخدام إنتاجي. استبعدتَهما من النطاق صراحةً، فتركتهما — لكن تسميتهما "غير مُعطِّلين" لن يكون دقيقاً: كلاهما يُعطّل شاشة كاملة عند أول فتح.

**ما لم يعد مُعطِّلاً:** لا Critical ولا High متبقّية، و`M6`/`M8` مُغلقان في المستودع.

**الحدّ الأدنى للوصول إلى NO:**
1. `firebase deploy --only firestore:indexes` والتأكّد من أن الفهرسين بلغا حالة **Enabled** في وحدة تحكّم Firebase.
2. ضبط `GOOGLE_CLIENT_ID` في بيئة الإنتاج (من `HIGH_FIXES.md`).
3. تنفيذ قائمة التحقّق اليدوية السبعة في `HIGH_FIXES.md` مقابل Firebase حقيقي.

لم أبدأ Production Validation، ولم أقترب من `main`.
