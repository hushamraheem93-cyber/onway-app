# OnWay — تقرير الفحص الشامل (Enterprise Audit)

> **تاريخ الفحص:** 2026-07-21
> **نطاق الفحص:** المستودع بالكامل — 199 مسار API، 164 ملف عميل، 23 ملف خادم، لوحة تحكم 11,009 سطر، قواعد Firebase، النشر، الاختبارات.
> **المرجع الوحيد:** المستودع نفسه (لا افتراضات خارجية).

---

## ملخص تنفيذي

| المحور | النتيجة (0–100) |
|--------|------------------|
| Architecture (البنية) | **62** |
| Backend (الخادم) | **68** |
| Frontend (الواجهات) | **70** |
| Security (الأمان) | **45** |
| Performance (الأداء) | **58** |
| Maintainability (قابلية الصيانة) | **55** |
| **Production Readiness (جاهزية الإنتاج)** | **52** |

**إجمالي النتائج:** 3 حرجة (Critical) · 10 عالية (High) · 14 متوسطة (Medium) · 8 منخفضة (Low)

**الخلاصة:** الأساس الوظيفي قوي (تسعير موثوق من الخادم، محرك تسوية مالية idempotent، حراسة JWT على مسارات العميل/السائق/التاجر). لكن **ثلاث مشاكل حرجة تمنع الإطلاق الآمن**: ثغرة XSS مخزّنة تسمح بالسيطرة على لوحة التحكم، وتجاوز كامل لمصادقة السائق عبر WebSocket، وميزة مالية معطّلة صامتة بسبب تعارض مسارات.

---

## 🔴 CRITICAL — حرجة (يجب إصلاحها قبل الإطلاق)

### C1 — ثغرة XSS مخزّنة في لوحة التحكم → سيطرة كاملة على المنصة
**الملف:** `server/templates/admin.html`
**الأسطر:** 10625، 10623، 8321، 7586، 8444، 9852، 10555، 10571، 10682، 10770، 5465، 5468

**السبب الجذري:**
اللوحة تحقن بيانات يتحكم بها المستخدم داخل `innerHTML` **دون تمريرها على `escapeHtml`** الموجودة أصلاً في نفس الملف. أخطرها السطر 10625:
```js
<td ... title="${(r.comment||'').replace(/"/g,'&quot;')}">${r.comment || '...'}</td>
```
`r.comment` هو **نص تقييم يكتبه الزبون بحرّية**. كذلك `storeName` (يتحكم به التاجر)، `customerName`، وأسماء المنتجات.
والأسوأ السطر 5468 — حقن داخل خاصية `onclick`:
```js
onclick="openTrackingModal('${loc.phoneNumber}','${loc.fullName||loc.phoneNumber}')"
```

**الخطر:**
زبون يكتب تقييماً يحتوي `<img src=x onerror="fetch('/api/admin/reset-month',{method:'POST'})">`. عند فتح الأدمن صفحة التقييمات، ينفَّذ الكود **داخل جلسة الأدمن**. الكوكي `HttpOnly` يمنع سرقته، لكنه **يُرسَل تلقائياً** مع أي طلب من نفس الأصل — فالمهاجم ينفّذ أي إجراء إداري: حذف الطلبات، تصفير المحافظ، اعتماد متاجر، تسريب كل بيانات الزبائن. **اختراق كامل للمنصة من حقل تقييم عام.**

**الإصلاح الموصى به:**
تمرير كل قيمة يتحكم بها المستخدم على `escapeHtml()` قبل الحقن، واستبدال الحقن داخل `onclick` بـ `data-*` + `addEventListener`. إضافة رأس `Content-Security-Policy` كطبقة دفاع ثانية.

---

### C2 — تجاوز كامل لمصادقة السائق عبر WebSocket
**الملف:** `server/routes.ts`
**الأسطر:** 6383–6419 (خاصة 6390)

**السبب الجذري:**
لا توجد **أي** مصادقة على اتصال socket.io (`io.use()` غير موجود)، والحدث `driver:location` يأخذ هوية السائق **من جسم الرسالة**:
```js
socket.on("driver:location", async ({ phoneNumber, lat, lng }) => {
  ...
  driverLocations.set(phoneNumber, {...});
  updateDriverLastLocation(phoneNumber, lat, lng);   // ← كتابة في Firestore
});
```
هذا يُبطل تماماً الحماية المبنية في `requireDriverAuth` (routes.ts:321) التي تفرض أخذ الهوية من التوكن الموقّع فقط.

**الخطر:**
أي شخص (بلا تسجيل دخول) يتصل بالـ socket ويرسل رقم هاتف أي سائق ليزوّر موقعه — يُعرَض للزبائن على الخريطة **ويُكتب في قاعدة البيانات**. احتيال في التتبّع، وإفساد بيانات التشغيل، وتسريب اسم السائق للغرف.

**الإصلاح الموصى به:**
إضافة `io.use()` يتحقق من توكن السائق الموقّع عند المصافحة (handshake)، وأخذ `phoneNumber` من التوكن حصراً — لا من الرسالة.

---

### C3 — ميزة «صافي أرباح التاجر» معطّلة صامتاً (مسار مظلَّل / Shadowed Route)
**الملفات:** `server/index.ts:920` و`:922` · `server/routes.ts:488` · `server/vendor.ts:1486` · `client/screens/VendorWalletScreen.tsx:291,307`

**السبب الجذري:**
```js
app.use(vendorRouter);              // index.ts:920  ← يُسجَّل أولاً
const server = await registerRoutes(app);  // index.ts:922
```
Express يطابق بترتيب التسجيل، فـ `vendor.ts` **يفوز دائماً**. يوجد `GET /api/vendor/wallet` مُعرَّف مرتين:
- `vendor.ts:1486` — **الحيّ**، يُرجع `{totalRevenue, totalOrders, avgOrderValue, dailySales, recentSales, period}` بلا عمولة.
- `routes.ts:488` — **ميت**، وهو الذي أُضيفت إليه `commissionRate` و`netEarning`.

والعميل `VendorWalletScreen.tsx:291` يعرض `sale.netEarning` — الذي لا يصل أبداً.

**الخطر:**
ميزة **مالية** بُنيت واعتُبرت منجزة لكنها لا تعمل إطلاقاً: التاجر لا يرى صافي ربحه ولا نسبة العمولة. أي قرار تجاري مبني عليها خاطئ. وهناك 3 مسارات مظلَّلة أخرى (`/api/stores`، `/api/stores/:id/products`، `/api/stores/products-preview`) تعني أن تعديلات `routes.ts` عليها لا أثر لها.

**الإصلاح الموصى به:**
نقل منطق العمولة إلى المسار الحيّ في `vendor.ts`، وحذف المسارات الميتة في `routes.ts` نهائياً لمنع تكرار الالتباس.

---

## 🟠 HIGH — عالية

### H1 — كشف بيانات السائقين الشخصية بلا مصادقة
**الملف:** `server/routes.ts:3058` — `GET /api/drivers/check/:phoneNumber`
**السبب:** مسار عام يُرجع الاسم الرباعي الكامل، الحالة، وتواريخ التسجيل لأي رقم هاتف.
**الخطر:** تعداد أرقام الهواتف (enumeration) + تسريب PII. يمكن بناء قاعدة بيانات كاملة بأسماء السائقين.
**الإصلاح:** تقييده بـ `requireCustomerAuth` مع ملكية الرقم (الرقم في التوكن = الرقم المطلوب)، والاكتفاء بإرجاع `{exists, status}`.

### H2 — إحراق أكواد الخصم لمستخدمين آخرين
**الملف:** `server/routes.ts:5265` — `POST /api/promo-codes/record-usage`
**السبب:** بلا مصادقة، ويأخذ `userId` من جسم الطلب.
**الخطر:** مهاجم يسجّل استخدام كود خصم نيابة عن أي مستخدم فيحرمه منه (griefing)، أو يُفسد إحصاءات الحملات.
**الإصلاح:** `requireCustomerAuth` + اشتقاق `userId` من التوكن، وتجاهل ما يرسله العميل.

### H3 — تسجيل دخول التاجر بكلمة مرور بلا حد للمحاولات
**الملف:** `server/index.ts:117–124` (جدول `LIMITS`) · المسار `server/vendor.ts:275`
**السبب:** `LIMITS` يحدّ `/api/vendor/mobile-auth` بـ 20/دقيقة لكن **لا يذكر `/api/vendor/login`** فيسقط على الافتراضي 600/دقيقة.
**الخطر:** تخمين كلمات مرور التجّار بـ 600 محاولة/دقيقة/IP.
**الإصلاح:** إضافة `"/api/vendor/login": 10` إلى `LIMITS`.

### H4 — أكواد OTP غير آمنة تشفيرياً وقصيرة
**الملف:** `server/firebase.ts:1335–1344`
**السبب:** `Math.floor(1000 + Math.random() * 9000)` — مولّد أرقام شبه عشوائي (غير تشفيري) وطول 4 خانات فقط (9000 احتمال).
**الخطر:** `Math.random()` قابل للتنبؤ عملياً بمعرفة حالة المولّد؛ ومساحة 4 خانات ضيقة. التخفيف الحالي (5 محاولات) جيّد لكنه لا يعالج قابلية التنبؤ.
**الإصلاح:** `crypto.randomInt(100000, 1000000)` (6 خانات).

### H5 — تسجيل سائق بلا مصادقة
**الملف:** `server/routes.ts:3087` — `POST /api/drivers`
**السبب:** أي شخص يسجّل سائقاً بأي رقم هاتف مع صور Base64.
**الخطر:** احتلال هوية أرقام حقيقية، وإغراق قاعدة البيانات بصور Base64 (كل مستند حتى 1MB) — استنزاف تخزين وتكلفة.
**الإصلاح:** `requireCustomerAuth` + مطابقة الرقم مع التوكن + حدّ معدّل خاص.

### H6 — الانضمام لغرفة أي طلب عبر WebSocket بلا مصادقة
**الملف:** `server/routes.ts:6385` — `socket.on("order:watch")`
**السبب:** يقبل أي `orderId` بلا تحقق ملكية.
**الخطر:** استقبال حالة الطلب **وموقع السائق المباشر** لأي طلب. مُخفَّف جزئياً لأن معرّفات Firestore عشوائية، لكنه IDOR حقيقي.
**الإصلاح:** التحقق من توكن العميل ومطابقة `order.phoneNumber` قبل `socket.join`.

### H7 — وكيل Google Maps مفتوح للجميع (استنزاف فوترة)
**الملف:** `server/routes.ts:5278` — `GET /api/reverse-geocode`
**السبب:** بلا مصادقة وبلا حدّ معدّل خاص؛ يستدعي Geocoding + Places بمفتاحك.
**الخطر:** أي طرف يستخدم مفتاح Google الخاص بك مجاناً → فاتورة ضخمة.
**الإصلاح:** `requireCustomerAuth` + حدّ معدّل صارم (مثلاً 30/دقيقة) + تخزين مؤقت للنتائج.

### H8 — مسح كامل لمجموعة الطلبات في إحصاءات التاجر
**الملف:** `server/vendor.ts:1070` · وأيضاً `server/routes.ts:5722`
**السبب:**
```js
? await db.collection("orders").get()     // vendor.ts:1070 — بلا where/limit
db.collection("orders").where("createdAt", ">=", since).orderBy(...).get()  // routes.ts:5722 — بلا limit
```
**الخطر:** مع نمو الطلبات، كل فتح لشاشة إحصاءات التاجر يقرأ **كل** الطلبات → بطء شديد، تكلفة قراءات Firestore عالية، واحتمال نفاد الذاكرة.
**الإصلاح:** فهرسة `vendorId` والاستعلام به مباشرة + `limit()` إلزامي + ترقيم صفحات.

### H9 — غياب Content-Security-Policy
**الملفات:** `deployment/nginx.conf:47–51` · `server/index.ts:227–233`
**السبب:** توجد رؤوس أمان جيدة (HSTS، nosniff، X-Frame-Options) لكن **لا CSP إطلاقاً**.
**الخطر:** يضاعف أثر C1 — بلا CSP لا يوجد أي حاجز أمام تنفيذ سكربت محقون.
**الإصلاح:** إضافة CSP على مسار `/admin` على الأقل.

### H10 — كوكي الأدمن بـ SameSite=None
**الملف:** `server/index.ts:526`
**السبب:** `POST /api/admin/login` يضبط الكوكي بـ `SameSite=None` (لدعم لوحة على نطاق مختلف).
**الخطر:** الكوكي يُرسَل مع طلبات من مواقع أخرى → خطر CSRF على كل `/api/admin/*`. مُخفَّف حالياً بفحص CORS الذي يرفض الأصول غير المسموحة (403)، لكنه دفاع هشّ يعتمد على ضبط `ALLOWED_ORIGINS` بدقة.
**الإصلاح:** استخدام توكن Bearer للنطاقات المختلفة والاكتفاء بـ `SameSite=Strict` للكوكي، أو إضافة رمز CSRF.

---

## 🟡 MEDIUM — متوسطة

| # | العنوان | الملف/السطر | السبب والخطر | الإصلاح |
|---|---------|--------------|---------------|---------|
| M1 | فهارس Firestore مركّبة ناقصة | `firestore.indexes.json` (5 فهارس فقط) مقابل استعلامات في `firebase.ts:1049`, `1435`, `routes.ts:2546`, `4545` | استعلامات `isOnline+status`, `userId+promoCode`, `driverId+status` قد تفشل وقت التشغيل بـ FAILED_PRECONDITION | إضافة الفهارس المركّبة الناقصة |
| M2 | موقع السائق بلا مصادقة | `routes.ts:3334` | من يملك `orderId` يتتبّع السائق واسمه | ربطه بتوكن العميل + ملكية الطلب |
| M3 | 4 مسارات مكرّرة مظلَّلة | `routes.ts:388,418,449,488` مقابل `vendor.ts:1578,1667,1711,1486` | كود ميت يوهم المطوّر أن تعديله ساري | حذف النسخ الميتة |
| M4 | شاشات ميتة | `VendorHomeScreen.tsx` (1410 سطر)، `StoresScreen.tsx` (576)، `OffersScreen.tsx` (86) | 2072 سطر غير مستخدم يزيد حجم الحزمة ويشتّت الصيانة | حذفها |
| M5 | طبقة API ميتة | `client/services/api.ts` (169 سطر، صفر مستوردين) | ثلاثة أنماط وصول للبيانات متوازية | حذفها أو توحيد الوصول عليها |
| M6 | تبعيات Drizzle/pg بلا أي جدول | `package.json:45,46,76,104`، `shared/schema.ts` (0 `pgTable`) | بنية تحتية مضلّلة + حجم تثبيت زائد | إزالة `drizzle-orm`, `drizzle-zod`, `drizzle-kit`, `pg`, `drizzle.config.ts` |
| M7 | 71MB صور مستخدمين داخل Git | `uploads/` (125 ملف)، `.gitignore` لا يتضمنها؛ `.git` = 197MB | استنساخ بطيء، تضخم دائم في التاريخ | تجاهلها + نقل التخزين إلى Firebase Storage |
| M8 | 63 كتلة `catch {}` صامتة | `client/` (أعلاها `AuthContext.tsx`: 11، `VendorHomeScreen`: 8) | الفشل يمرّ بلا رسالة ولا إعادة محاولة → المستخدم لا يفهم سبب التعطّل | رسالة عربية + إعادة محاولة في المسارات الحرجة |
| M9 | لا Skeletons رغم وجود المكوّن | 37 شاشة تستخدم `ActivityIndicator`، `client/components/Skeleton.tsx` صفر استخدام | إحساس بطء | تبنّي Skeleton في الشاشات الرئيسية |
| M10 | SEO شبه معدوم | `server/templates/landing-page.html` — `<title>APP_NAME_PLACEHOLDER</title>` فقط | لا وصف، لا OG، لا canonical، لا robots → غياب من نتائج البحث والمشاركات | إضافة وسوم SEO كاملة |
| M11 | لا مُشغّل اختبارات ولا CI | `package.json` بلا `test`؛ لا `.github/workflows` رغم وجود `tests/api/*` | الانحدارات لا تُكتشف | إضافة `npm test` + CI (`check:types` + `lint` + tests) |
| M12 | مقارنة كلمة مرور الأدمن نصية | `server/index.ts` (`validateAdminCredentials`) | `password === validPass` غير محصّنة زمنياً (timing) للمسار الاحتياطي | `crypto.timingSafeEqual` |
| M13 | سرّ احتياطي في adminAuth | `server/adminAuth.ts` — `JWT_SECRET \|\| "dev-admin-secret-not-for-production"` | خامل حالياً (الخادم يرفض الإقلاع بلا `JWT_SECRET`) لكنه فخّ مستقبلي | إزالة الاحتياطي والاعتماد على الرمي |
| M14 | إتاحة وصول ضعيفة | 114 `accessibilityLabel` مقابل 406 عنصر قابل للضغط (~28%) | أزرار الأيقونات بلا وصف لقارئ الشاشة | إضافة تسميات للأزرار الأيقونية |

---

## 🟢 LOW — منخفضة

| # | العنوان | الملف | ملاحظة |
|---|---------|-------|--------|
| L1 | ملفات عملاقة | `admin.html` (11,009)، `routes.ts` (6,740)، `AdminScreen.tsx` (4,067)، `firebase.ts` (2,611) | صعوبة مراجعة وتعارضات دمج؛ يُنصح بالتقسيم |
| L2 | 68 لوناً ثابتاً (hex) | `client/screens`, `components`, `context` | يكسر الوضع الداكن جزئياً |
| L3 | مسح كامل لمجموعات صغيرة | `firebase.ts:345,1927`، `routes.ts:5609–5612` | مقبول الآن، يصبح مشكلة مع النمو |
| L4 | منطق نطاق فرعي في CORS لا يعمل | `server/index.ts` (`origin.endsWith('.' + d)`) | يفشل مغلقاً (آمن) لكنه مضلّل عند الضبط |
| L5 | ملفات تقارير داخل المستودع | `financial-audit-report.txt`, `production-readiness-audit.md`, `tests/reports/*.html` | ضجيج؛ ينبغي تجاهلها |
| L6 | نمو غير محدود لمخزن حدّ المعدّل | `server/index.ts:103` | التنظيف كل 5 دقائق فقط |
| L7 | حالة تشغيلية في الذاكرة | طابور السائقين، الجلسات، حدّ المعدّل | موثّق في `ecosystem.config.js` (`instances: 1`) — يمنع التوسّع الأفقي |
| L8 | نسخة Firebase مكرّرة | `firebaseConfig.js` (جذر) و`client/lib/firebase.ts` | الأول غير مستخدم؛ يُحذف |

---

## ✅ نقاط القوة (تستحق الحفاظ عليها)

1. **التسعير موثوق من الخادم بالكامل** (`routes.ts` — إعادة جلب الأسعار والتحقق من الإجمالي) — أقوى جزء في المشروع.
2. **محرك التسوية المالية** (`server/settlement.ts`) — معاملات ذرّية و**idempotent** بمعرّف `orderId__accountType`؛ لا ازدواج احتساب.
3. **حراسة JWT شاملة** — `requireCustomerAuth` / `requireDriverAuth` / `requireVendor` / `requireAdminAuth` مطبّقة على مسارات HTTP مع فحص ملكية.
4. **جلسات الأدمن JWT موقّعة** مع إبطال (`jti` + `revokedBefore`) — تنجو من إعادة تشغيل الخادم.
5. **CORS يفشل مغلقاً في الإنتاج**، ومعالج الأخطاء لا يسرّب تفاصيل داخلية.
6. **حدّ المعدّل يعتمد آخر إدخال في X-Forwarded-For** (لا يمكن انتحاله) وnginx يضبطه صحيحاً.
7. **قواعد Firestore افتراضها الرفض** — البيانات المالية والحساسة غير قابلة للقراءة من العميل.
8. **PM2 مضبوط بوعي** (`instances: 1`) مع تعليق يشرح سبب منع التوسّع.

---

## خطة الإصلاح حسب الأولوية

**المرحلة 1 — Critical (قبل أي إطلاق):** C1 (XSS) → C2 (WebSocket) → C3 (المسار المظلَّل).
**المرحلة 2 — High:** H1, H2, H3, H4, H5, H6, H7 (أمان) ثم H8 (أداء) ثم H9, H10.
**المرحلة 3 — Medium:** M1–M14 حسب الترتيب.
**المرحلة 4 — Low:** تنظيف وتقسيم الملفات العملاقة.

---

*انتهى التقرير.*
