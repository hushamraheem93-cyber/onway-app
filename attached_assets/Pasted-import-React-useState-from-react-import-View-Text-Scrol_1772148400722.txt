import React, { useState } from 'react';
import { View, Text, ScrollView, TouchableOpacity, StyleSheet, CheckBox } from 'react-native';

const DriverAgreement = ({ onAccept }) => {
  const [isAccepted, setIsAccepted] = useState(false);

  return (
    <View style={styles.container}>
      <Text style={styles.headerTitle}>اتفاقية انضمام كابتن OnWay</Text>
      
      {/* صندوق القوانين القابل للتمرير */}
      <ScrollView style={styles.rulesBox}>
        <Text style={styles.ruleText}>
          بصفتك كابتن في تطبيق OnWay داخل قضاء الضلوعية، يجب عليك الالتزام بالشروط التالية: {"\n\n"}
          
          <Text style={styles.bold}>1. الأمانة والمسؤولية:</Text> {"\n"}
          أتعهد بالحفاظ على الطلبات وتسليمها بحالتها الأصلية دون فتح الغلاف أو التلاعب بالمحتويات، وأتحمل المسؤولية الكاملة عن أي نقص أو تلف يحدث للطلب أثناء النقل. {"\n\n"}

          <Text style={styles.bold}>2. التعامل الأخلاقي:</Text> {"\n"}
          الالتزام بالأدب وحسن السيرة والسلوك مع الزبائن وأصحاب المحلات، وتمثيل تطبيق OnWay بأفضل صورة أمام أهالي المنطقة. {"\n\n"}

          <Text style={styles.bold}>3. السلامة المرورية:</Text> {"\n"}
          أتعهد بالالتزام بقواعد السلامة المرورية أثناء القيادة، وأقر بأنني المسؤول الأول والقانوني عن أي حادث أو مخالفة مرورية تحدث أثناء العمل. {"\n\n"}

          <Text style={styles.bold}>4. خصوصية البيانات:</Text> {"\n"}
          أتعهد بعدم استخدام أرقام هواتف الزبائن أو مواقع سكنهم لأي غرض خارج إطار عملية التوصيل، ويمنع التواصل مع الزبون بعد انتهاء الطلب نهائياً. {"\n\n"}

          <Text style={styles.bold}>5. إخلاء المسؤولية:</Text> {"\n"}
          يخلي تطبيق OnWay مسؤوليته عن أي نزاعات قانونية أو حوادث قد يتعرض لها السائق، حيث يعتبر السائق متعاقداً مستقلاً ويتحمل كافة التبعات القانونية لعمله.
        </Text>
      </ScrollView>

      {/* خيار الموافقة */}
      <TouchableOpacity 
        style={styles.checkboxWrapper} 
        onPress={() => setIsAccepted(!isAccepted)}
      >
        <View style={[styles.checkbox, isAccepted && styles.checkboxChecked]} />
        <Text style={styles.checkboxLabel}>
          أقر بأنني قرأت كافة الشروط وأوافق على تحمل المسؤولية الكاملة.
        </Text>
      </TouchableOpacity>

      {/* زر الاستمرار */}
      <TouchableOpacity 
        style={[styles.btn, !isAccepted && styles.btnDisabled]} 
        disabled={!isAccepted}
        onPress={onAccept}
      >
        <Text style={styles.btnText}>إتمام الانضمام للفريق</Text>
      </TouchableOpacity>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, padding: 20, backgroundColor: '#FFF' },
  headerTitle: { fontSize: 20, fontWeight: 'bold', color: '#F37335', textAlign: 'center', marginBottom: 20 },
  rulesBox: { flex: 1, backgroundColor: '#F9F9F9', borderRadius: 10, padding: 15, borderWidth: 1, borderColor: '#EEE' },
  ruleText: { fontSize: 14, color: '#333', textAlign: 'right', lineHeight: 22 },
  bold: { fontWeight: 'bold', color: '#000' },
  checkboxWrapper: { flexDirection: 'row-reverse', alignItems: 'center', marginVertical: 20 },
  checkbox: { width: 20, height: 20, borderWidth: 2, borderColor: '#F37335', borderRadius: 4, marginLeft: 10 },
  checkboxChecked: { backgroundColor: '#F37335' },
  checkboxLabel: { fontSize: 13, color: '#666', flex: 1, textAlign: 'right' },
  btn: { backgroundColor: '#F37335', padding: 15, borderRadius: 10, alignItems: 'center' },
  btnDisabled: { backgroundColor: '#CCC' },
  btnText: { color: '#FFF', fontSize: 16, fontWeight: 'bold' }
});

export default DriverAgreement;
