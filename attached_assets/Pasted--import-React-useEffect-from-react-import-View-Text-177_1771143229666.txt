صمملي شكل الواجهة هذه  هذه الكود 

import React, { useEffect } from 'react';
import {
  View,
  Text,
  StyleSheet,
  Image,
  TouchableOpacity,
  Dimensions,
} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { LinearGradient } from 'expo-linear-gradient';
import { useNavigation } from '@react-navigation/native';
import type { NativeStackNavigationProp } from '@react-navigation/native-stack';

const { width, height } = Dimensions.get('window');

type RootStackParamList = {
  Home: undefined;
};

type NavigationProp = NativeStackNavigationProp<RootStackParamList>;

export default function SplashScreen() {
  const navigation = useNavigation<NavigationProp>();

  return (
    <SafeAreaView style={styles.container}>
      <LinearGradient
        colors={['#F4B942', '#F4B942']}
        style={styles.gradient}
      >
        {/* App Title */}
        <Text style={styles.title}>OnWay App</Text>

        {/* Delivery Person Image */}
        <View style={styles.imageContainer}>
          <Text style={styles.deliveryEmoji}>🛵</Text>
          <Text style={styles.boxEmoji}>📦</Text>
        </View>

        {/* Dots Indicator */}
        <View style={styles.dotsContainer}>
          <View style={[styles.dot, styles.activeDot]} />
          <View style={styles.dot} />
          <View style={styles.dot} />
        </View>

        {/* Tagline */}
        <View style={styles.taglineContainer}>
          <Text style={styles.tagline}>احصل على طلبك</Text>
          <Text style={styles.taglineBold}>في أي مكان، في أي وقت</Text>
        </View>

        {/* Get Started Button */}
        <TouchableOpacity
          style={styles.button}
          onPress={() => navigation.navigate('Home')}
          activeOpacity={0.9}
        >
          <LinearGradient
            colors={['#FF6B6B', '#FF8E8E']}
            style={styles.buttonGradient}
            start={{ x: 0, y: 0 }}
            end={{ x: 1, y: 0 }}
          >
            <Text style={styles.buttonText}>ابدأ الآن</Text>
          </LinearGradient>
        </TouchableOpacity>
      </LinearGradient>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  gradient: {
    flex: 1,
    alignItems: 'center',
    paddingHorizontal: 20,
  },
  title: {
    fontFamily: 'Cairo_700Bold',
    fontSize: 32,
    color: '#FFFFFF',
    marginTop: 40,
    letterSpacing: 1,
  },
  imageContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 40,
  },
  deliveryEmoji: {
    fontSize: 180,
    textAlign: 'center',
  },
  boxEmoji: {
    fontSize: 60,
    position: 'absolute',
    top: 40,
    right: -20,
  },
  dotsContainer: {
    flexDirection: 'row',
    gap: 8,
    marginBottom: 30,
  },
  dot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    backgroundColor: 'rgba(255, 255, 255, 0.4)',
  },
  activeDot: {
    width: 24,
    backgroundColor: '#FFFFFF',
  },
  taglineContainer: {
    alignItems: 'center',
    marginBottom: 40,
  },
  tagline: {
    fontFamily: 'Cairo_400Regular',
    fontSize: 24,
    color: '#2C3E50',
    textAlign: 'center',
  },
  taglineBold: {
    fontFamily: 'Cairo_700Bold',
    fontSize: 28,
    color: '#2C3E50',
    textAlign: 'center',
    fontStyle: 'italic',
  },
  button: {
    width: width - 40,
    borderRadius: 16,
    overflow: 'hidden',
    marginBottom: 40,
    shadowColor: '#FF6B6B',
    shadowOffset: { width: 0, height: 6 },
    shadowOpacity: 0.3,
    shadowRadius: 10,
    elevation: 10,
  },
  buttonGradient: {
    paddingVertical: 18,
    alignItems: 'center',
  },
  buttonText: {
    fontFamily: 'Cairo_700Bold',
    fontSize: 20,
    color: '#FFFFFF',
  },
});
