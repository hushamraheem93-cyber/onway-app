import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
// 1. استيراد مكتبات الأيقونات (ستحتاج لتثبيت react-native-vector-icons)
import FontAwesome5 from 'react-native-vector-icons/FontAwesome5'; // للدراجة النارية
import MaterialIcons from 'react-native-vector-icons/MaterialIcons'; // للمستخدم

const RoleSelectionScreen = ({ navigation }) => {
  return (
    <View style={styles.container}>
      {/* 2. رأس الصفحة البرتقالي (كالسابق) */}
      <View style={styles.header}>
        <TouchableOpacity style={styles.backButton} onPress={() => navigation.goBack()}>
          <MaterialIcons name="arrow-back" size={24} color="#FFFFFF" />
        </TouchableOpacity>
        <Text style={styles.logoText}>OnWay</Text>
        <Text style={styles.mainTitle}>كيف تود استخدام التطبيق؟</Text>
        <Text style={styles.subTitle}>اختر نوع حسابك للمتابعة</Text>
      </View>

      {/* 3. حاوية البطاقات البيضاء */}
      <View style={styles.cardContainer}>
        
        {/* بطاقة الزبون المحدثة */}
        <TouchableOpacity style={styles.card} onPress={() => navigation.navigate('CustomerLogin')}>
          <MaterialIcons name="keyboard-arrow-left" size={24} color="#999" />
          <View style={styles.cardTextContainer}>
            <Text style={styles.cardTitle}>زبون</Text>
            <Text style={styles.cardSubTitle}>تصفح المنتجات واطلب التوصيل لباب منزلك</Text>
          </View>
          {/* 4. تغيير أيقونة الزبون */}
          <View style={[styles.iconWrapper, { backgroundColor: '#FFEDD8' }]}>
            <MaterialIcons name="person" size={30} color="#F37335" />
          </View>
        </TouchableOpacity>

        {/* بطاقة سائق التوصيل المحدثة */}
        <TouchableOpacity style={styles.card} onPress={() => navigation.navigate('DriverLogin')}>
          <MaterialIcons name="keyboard-arrow-left" size={24} color="#999" />
          <View style={styles.cardTextContainer}>
            <Text style={styles.cardTitle}>سائق توصيل</Text>
            <Text style={styles.cardSubTitle}>انضم لفريق التوصيل واكسب المال بتوصيل الطلبات</Text>
          </View>
          {/* 5. تغيير أيقونة سائق التوصيل */}
          <View style={[styles.iconWrapper, { backgroundColor: '#E0F2F1' }]}>
            <FontAwesome5 name="motorcycle" size={30} color="#009688" />
          </View>
        </TouchableOpacity>

      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#FFFFFF' },
  header: {
    backgroundColor: '#F37335', // البرتقالي الدافئ
    paddingTop: 60,
    paddingBottom: 40,
    paddingHorizontal: 25,
    borderBottomLeftRadius: 30,
    borderBottomRightRadius: 30,
  },
  backButton: { marginBottom: 20 },
  logoText: { color: '#FFFFFF', fontSize: 24, fontWeight: 'bold', textAlign: 'center', marginBottom: 20 },
  mainTitle: { color: '#FFFFFF', fontSize: 20, fontWeight: 'bold', textAlign: 'center', marginBottom: 10 },
  subTitle: { color: '#FFFFFF', fontSize: 14, textAlign: 'center' },
  cardContainer: { padding: 20 },
  card: {
    flexDirection: 'row',
    backgroundColor: '#FFFFFF',
    borderRadius: 20,
    padding: 15,
    marginBottom: 20,
    elevation: 5,
    shadowColor: '#000000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 5,
    alignItems: 'center',
    justifyContent: 'space-between',
    borderWidth: 1,
    borderColor: '#EEEEEE',
  },
  cardTextContainer: { flex: 1, paddingRight: 15 },
  cardTitle: { fontSize: 18, fontWeight: 'bold', color: '#000000', marginBottom: 5 },
  cardSubTitle: { fontSize: 12, color: '#666' },
  iconWrapper: {
    width: 60,
    height: 60,
    borderRadius: 20,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default RoleSelectionScreen;
