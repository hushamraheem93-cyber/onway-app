import React from 'react';
import { View, Text, TextInput, TouchableOpacity, StyleSheet, Image, KeyboardAvoidingView, Platform } from 'react-native';
import { LinearGradient } from 'expo-linear-gradient'; // تأكد من تثبيت هذه المكتبة في ريبلت
import { FontAwesome } from '@expo/vector-icons';

const LoginScreen = () => {
  return (
    <LinearGradient colors={['#FF8C00', '#FF6B00']} style={styles.container}>
      <KeyboardAvoidingView behavior={Platform.OS === "ios" ? "padding" : "height"} style={styles.inner}>
        
        {/* الشعار */}
        <View style={styles.logoContainer}>
          <Text style={styles.logoText}>OnWay</Text>
          <Text style={styles.logoSubText}>أون وي</Text>
        </View>

        {/* نموذج تسجيل الدخول */}
        <View style={styles.formContainer}>
          <Text style={styles.loginTitle}>تسجيل الدخول</Text>
          
          <View style={styles.inputWrapper}>
            <TextInput 
              placeholder="009647xxxxxxxxx"
              placeholderTextColor="#AAA"
              keyboardType="phone-pad"
              style={styles.input}
            />
            <FontAwesome name="phone" size={20} color="#FF6B00" style={styles.inputIcon} />
          </View>

          <TouchableOpacity style={styles.mainButton}>
            <Text style={styles.buttonText}>متابعة</Text>
          </TouchableOpacity>
        </View>

        {/* الشروط والروابط الاجتماعية */}
        <View style={styles.footer}>
          <Text style={styles.termsText}>
            بالتتابعة، أنت توافق على شروط الخدمة وسياسة الخصوصية
          </Text>
          
          <View style={styles.socialRow}>
             <TouchableOpacity style={styles.socialIcon}><FontAwesome name="facebook" size={24} color="white" /></TouchableOpacity>
             <TouchableOpacity style={styles.socialIcon}><FontAwesome name="instagram" size={24} color="white" /></TouchableOpacity>
          </View>
        </View>

      </KeyboardAvoidingView>
    </LinearGradient>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1 },
  inner: { flex: 1, justifyContent: 'center', padding: 30 },
  logoContainer: { alignItems: 'center', marginBottom: 50 },
  logoText: { fontSize: 45, fontWeight: '900', color: 'white', letterSpacing: 2 },
  logoSubText: { fontSize: 20, color: 'white', marginTop: -5, opacity: 0.9 },
  formContainer: { backgroundColor: 'rgba(255, 255, 255, 0.15)', padding: 25, borderRadius: 25, backdropFilter: 'blur(10px)' },
  loginTitle: { color: 'white', fontSize: 18, textAlign: 'center', marginBottom: 20, fontWeight: 'bold' },
  inputWrapper: { flexDirection: 'row', backgroundColor: 'white', borderRadius: 15, alignItems: 'center', paddingHorizontal: 15, height: 55 },
  input: { flex: 1, textAlign: 'right', fontSize: 16, color: '#333' },
  inputIcon: { marginLeft: 10 },
  mainButton: { backgroundColor: 'white', height: 55, borderRadius: 15, justifyContent: 'center', alignItems: 'center', marginTop: 20 },
  buttonText: { color: '#FF6B00', fontSize: 18, fontWeight: 'bold' },
  footer: { position: 'absolute', bottom: 40, width: '100%', alignSelf: 'center', alignItems: 'center' },
  termsText: { color: 'white', textAlign: 'center', fontSize: 12, opacity: 0.8, marginBottom: 20 },
  socialRow: { flexDirection: 'row', gap: 20 },
  socialIcon: { padding: 10, backgroundColor: 'rgba(255,255,255,0.2)', borderRadius: 50 }
});

export default LoginScreen;
