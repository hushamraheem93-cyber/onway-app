import React, { useRef, useState } from 'react';
import { View, Text, TextInput, StyleSheet, TouchableOpacity, KeyboardAvoidingView, Platform } from 'react-native';

const OTPScreen = () => {
  const [otp, setOtp] = useState(['', '', '', '']);
  const inputs = useRef([]);

  const handleChange = (text, index) => {
    const newOtp = [...otp];
    newOtp[index] = text;
    setOtp(newOtp);

    // الانتقال التلقائي للمربع التالي
    if (text && index < 3) {
      inputs.current[index + 1].focus();
    }
  };

  const handleKeyPress = (e, index) => {
    // العودة للمربع السابق عند المسح (Backspace)
    if (e.nativeEvent.key === 'Backspace' && !otp[index] && index > 0) {
      inputs.current[index - 1].focus();
    }
  };

  return (
    <KeyboardAvoidingView 
      behavior={Platform.OS === "ios" ? "padding" : "height"} 
      style={styles.container}
    >
      <Text style={styles.title}>تأكيد الرمز</Text>
      <Text style={styles.subtitle}>أدخل الرمز المكون من 4 أرقام المرسل إلى هاتفك</Text>

      <View style={styles.otpContainer}>
        {otp.map((digit, index) => (
          <TextInput
            key={index}
            style={styles.otpInput}
            keyboardType="number-pad"
            maxLength={1}
            onChangeText={(text) => handleChange(text, index)}
            onKeyPress={(e) => handleKeyPress(e, index)}
            ref={(ref) => inputs.current[index] = ref}
            value={digit}
          />
        ))}
      </View>

      <TouchableOpacity style={styles.verifyButton}>
        <Text style={styles.verifyText}>تأكيد</Text>
      </TouchableOpacity>

      <View style={styles.resendContainer}>
        <Text style={styles.resendText}>لم يصلك الرمز؟ </Text>
        <TouchableOpacity>
          <Text style={styles.resendLink}>إعادة إرسال</Text>
        </TouchableOpacity>
      </View>
    </KeyboardAvoidingView>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#FF6B00', alignItems: 'center', justifyContent: 'center', padding: 20 },
  title: { fontSize: 24, fontWeight: 'bold', color: '#FFF', marginBottom: 10, fontFamily: 'Cairo-Bold' },
  subtitle: { fontSize: 16, color: '#FFF', textAlign: 'center', marginBottom: 30, opacity: 0.9 },
  otpContainer: { flexDirection: 'row', justifyContent: 'space-between', width: '80%', marginBottom: 40 },
  otpInput: {
    width: 60,
    height: 60,
    backgroundColor: '#FFF',
    borderRadius: 15,
    textAlign: 'center',
    fontSize: 24,
    fontWeight: 'bold',
    color: '#FF6B00',
    elevation: 5,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.2,
  },
  verifyButton: {
    backgroundColor: '#FFF',
    width: '100%',
    height: 55,
    borderRadius: 15,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 20
  },
  verifyText: { color: '#FF6B00', fontSize: 18, fontWeight: 'bold' },
  resendContainer: { flexDirection: 'row-reverse' },
  resendText: { color: '#FFF', fontSize: 14 },
  resendLink: { color: '#FFF', fontWeight: 'bold', textDecorationLine: 'underline' }
});

export default OTPScreen;