<View style={styles.tabsContainer}>
  <View style={styles.tabsBackground}>
    <TouchableOpacity
      style={[
        styles.tabButton,
        selectedCategory === 'restaurants' && styles.tabButtonActive,
      ]}
      onPress={() => setSelectedCategory('restaurants')}
      activeOpacity={0.8}
    >
      {selectedCategory === 'restaurants' ? (
        <LinearGradient
          colors={['#FF6B35', '#FF8C61']}
          style={styles.tabGradient}
          start={{ x: 0, y: 0 }}
          end={{ x: 1, y: 0 }}
        >
          <Text style={styles.tabIconActive}>🍽️</Text>
          <Text style={styles.tabTextActive}>21 مطاعم</Text>
        </LinearGradient>
      ) : (
        <>
          <Text style={styles.tabIcon}>🍽️</Text>
          <Text style={styles.tabText}>21 مطاعم</Text>
        </>
      )}
    </TouchableOpacity>

    <TouchableOpacity
      style={[
        styles.tabButton,
        selectedCategory === 'stores' && styles.tabButtonActive,
      ]}
      onPress={() => setSelectedCategory('stores')}
      activeOpacity={0.8}
    >
      {selectedCategory === 'stores' ? (
        <LinearGradient
          colors={['#FF6B35', '#FF8C61']}
          style={styles.tabGradient}
          start={{ x: 0, y: 0 }}
          end={{ x: 1, y: 0 }}
        >
          <Text style={styles.tabIconActive}>🛒</Text>
          <Text style={styles.tabTextActive}>متاجر</Text>
        </LinearGradient>
      ) : (
        <>
          <Text style={styles.tabIcon}>🛒</Text>
          <Text style={styles.tabText}>متاجر</Text>
        </>
      )}
    </TouchableOpacity>
  </View>
</View>

// Styles
tabsContainer: {
  paddingHorizontal: 4,
},

tabsBackground: {
  flexDirection: 'row',
  backgroundColor: '#F8F9FA',
  borderRadius: 16,
  padding: 4,
  gap: 4,
},

tabButton: {
  flex: 1,
  paddingVertical: 12,
  paddingHorizontal: 16,
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'center',
  gap: 8,
  borderRadius: 12,
},

tabButtonActive: {
  shadowColor: '#FF6B35',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.25,
  shadowRadius: 4,
  elevation: 4,
},

tabGradient: {
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'center',
  gap: 8,
  paddingVertical: 12,
  paddingHorizontal: 16,
  borderRadius: 10,
  position: 'absolute',
  top: 0,
  left: 0,
  right: 0,
  bottom: 0,
},

tabIcon: {
  fontSize: 18,
},

tabIconActive: {
  fontSize: 18,
},

tabText: {
  fontFamily: 'Cairo_600SemiBold',
  fontSize: 14,
  color: '#7F8C8D',
},

tabTextActive: {
  fontFamily: 'Cairo_700Bold',
  fontSize: 14,
  color: '#FFFFFF',
},
