# CRITICAL_FIXES.md — إغلاق الأخطاء الحرجة الستة

**المرجع الوحيد:** `FINAL_SADD_AUDIT.md`
**النطاق:** الأخطاء الحرجة (Critical) المؤكَّدة الستة **فقط**. لم يُلمس أي بند High أو Medium.
**التاريخ:** 2026-07-26
**الفرع:** `claude/onway-enterprise-review-q1d18o`

---

## ملخّص تنفيذي

| # | العنوان | الحالة | ملفات مُعدَّلة | اختبار انحدار |
|---|---------|--------|----------------|----------------|
| C1 | تجاوز مصادقة السائق (Account takeover) | ✅ مُغلق | `server/routes.ts` | 3 |
| C4 | XSS مخزَّن في محادثة الدعم بلوحة التحكم | ✅ مُغلق | `server/templates/admin.html` | 5 |
| C5 | عدم التحقق من `aud` في دخول Google للأدمن | ✅ مُغلق | `server/index.ts` | 3 |
| C2 | دفع زائد للتاجر في تسويات المتجر | ✅ مُغلق | `server/settlement.ts`, `server/routes.ts` | 6 |
| C3 | فجوة استرداد التسوية (فقدان مال نهائي) | ✅ مُغلق | `server/routes.ts` | 2 |
| C6 | تعليق الطلب في حالة غير متّسقة | ✅ مُغلق | `server/routes.ts` | 3 |

- **الأخطاء الحرجة المُغلقة:** 6 / 6
- **الأخطاء الحرجة المتبقّية:** 0
- **اختبارات جديدة:** 22 (ملف `tests/unit/critical-regressions.test.mjs`)
- **مجموع اختبارات الوحدة:** 71 ناجحة / 0 فاشلة (كانت 49)
- **اختبار الطفرات (Mutation testing):** 7/7 طفرات أُعيد فيها الخلل ⇒ المجموعة تنقلب إلى أحمر في كل حالة

---

## C1 — تجاوز مصادقة السائق (Driver authentication bypass)

**الملف:** `server/routes.ts` — `POST /api/driver/mobile-auth`

### السبب الجذري
كان المسار يُصدر **توكن سائق صالحاً** لأي رقم هاتف يوجد له سجل في مجموعة `drivers`، دون أي إثبات OTP. أُضيف هذا المسار الاحتياطي سابقاً لتفادي "قفل" السائق عند انتهاء صلاحية توكن العميل (30 يوماً) — لكنه في الواقع استيلاء كامل على الحساب: أرقام هواتف السائقين مرئية للعملاء داخل التطبيق وتظهر في مستندات الطلبات، فأي شخص يعرف رقماً يستطيع سكّ توكن سائق، وقراءة محفظته وأرباحه، والاتصال (online)، وقبول دفعات، ورؤية عناوين العملاء وإحداثياتهم، وتعليم الطلبات "مُسلَّمة" على دفتر السائق الحقيقي.

هذا **انحدار** (regression): كان مُصلَحاً سابقاً ثم عاد.

### التغيير
حُذف المسار الاحتياطي بالكامل. الطريق الوحيد للحصول على توكن سائق هو توكن عميل صالح **يطابق رقمه** الرقم المطلوب:

```ts
if (!verifiedPhone || verifiedPhone !== String(phoneNumber)) {
  return res.status(401).json({ error: "غير مصرح — يرجى التحقق من رقم الهاتف أولاً" });
}
```

مع تعليق صريح في الكود يشرح لماذا لا يجوز إعادة إضافة أي fallback.

### لماذا الإصلاح آمن
- لا تغيير في شكل الطلب أو الاستجابة عند النجاح — نفس `{ token, driver }`.
- سيناريو انتهاء الصلاحية مُعالَج أصلاً على العميل: `client/services/driverAuth.ts` يعيد تبادل توكن العميل المخزَّن تلقائياً عند 401. إن كان توكن العميل نفسه منتهياً، فإعادة OTP هي المسار الصحيح والمقصود.
- السائق غير المسجَّل ما زال يتلقّى `{ driver: null, token: null }` كما قبل.

### اختبار الانحدار
`describe("C1 — driver mobile-auth must require a phone-matching customer JWT")` — 3 اختبارات:
1. وجود المعالج فعلاً (حتى لا تمرّ الاختبارات على معالج غائب).
2. كتلة الرفض تُرجع 401 ولا تحتوي `makeDriverToken`.
3. لا يوجد أي نمط `if (!existingDriver) … makeDriverToken` — أي أن وجود سجل سائق لا يحلّ محلّ إثبات الـ OTP.

**طفرة:** إعادة الـ fallback ⇒ يفشل الاختبار 3. ✅

---

## C4 — XSS مخزَّن في محادثة الدعم (لوحة التحكم)

**الملف:** `server/templates/admin.html`

### السبب الجذري
وحدة محادثة الدعم في لوحة التحكم كانت تبني HTML بالكامل عبر قوالب نصية و`innerHTML`، **بصفر استدعاء** لأي دالة تهريب — رغم وجود `escapeHtml`/`escapeAttr` مستخدمتين في بقية اللوحة. كل حقل من هذه الحقول يكتبه العميل مباشرة عبر `POST /api/support/messages`. أي عميل موثَّق بـ OTP كان يستطيع إرسال رسالة دعم تحتوي `<img src=x onerror=…>` وتُنفَّذ في جلسة الأدمن عند فتح المحادثة — أي تنفيذ كود بصلاحيات الأدمن الكاملة (نفس أصل اللوحة، والكوكي `HttpOnly` لا يمنع إصدار طلبات إدارية من داخل الصفحة).

هذا أيضاً **انحدار**: جولة تهريب سابقة أصلحت 9 منافذ في اللوحة لكنها تخطّت وحدة الدعم كلياً.

### التغيير
تهريب **8 منافذ** (sinks):

| الموضع | قبل | بعد |
|--------|-----|-----|
| اسم العميل في القائمة | `${c.userName\|\|'مستخدم'}` | `escapeHtml(...)` |
| معاينة آخر رسالة | `${c.lastMessage\|\|''}` | `escapeHtml(...)` |
| الحرف الأول في الأفاتار | `${(c.userName\|\|'م').charAt(0)}` | `escapeHtml(...)` |
| وسيطات `onclick` | `'${c.phoneNumber}'` (اقتباس مفرد + `.replace(/'/g,…)` غير كافٍ) | `&quot;${escapeAttr(...)}&quot;` |
| نص الرسالة | `${m.text \|\| ''}` | `escapeHtml(m.text \|\| '')` |
| صورة الرسالة | `<img src="${imgSrc}"` | `escapeAttr(imgSrc)` |
| صورة المنتج | `<img src="${p.image \| p.imageUrl}"` | `escapeAttr(...)` |
| اسم المنتج | `${p.name \|\| ''}` | `escapeHtml(...)` |

### لماذا الإصلاح آمن
- `escapeHtml`/`escapeAttr` موجودتان مسبقاً في نفس الملف ومستخدمتان في وحدات أخرى — لا كود جديد ولا اعتماديات.
- التهريب يغيّر التمثيل فقط، لا البيانات: النص العربي والمسافات والرموز تُعرض كما هي؛ ما يتغيّر هو أن `<` و`&` و`"` لم تعد تُفسَّر كوسوم.
- سمة `onclick` تحوّلت من اقتباس مفرد هشّ إلى `&quot;` مع تهريب السمة — وهو ما يمنع الخروج من السمة، بينما استدعاء `openSupportChat` نفسه لم يتغيّر توقيعه.

### اختبار الانحدار
`describe("C4 — admin support chat must escape all user-controlled data")` — 5 اختبارات: أربعة تمسح الملف بحثاً عن أي `${…}` يحتوي `m.text` / `c.lastMessage` / `c.userName` / `p.name` بدون `escapeHtml`/`escapeAttr`، وخامس يتأكد أن وحدة `renderSupportMessages` تستدعي دوال التهريب فعلاً (لأن الحالة السابقة كانت "صفر استدعاءات").

**طفرة:** إزالة التهريب عن `m.text` ⇒ يفشل اختبار `m.text`. ✅

---

## C5 — عدم التحقق من جمهور التوكن (`aud`) في دخول Google للأدمن

**الملف:** `server/index.ts` — `POST /admin/google-signin`

### السبب الجذري
كان التحقق مكتوباً هكذا:

```ts
if (expectedClientId && payload.aud !== expectedClientId) { … }
```

أي أنه **يُتخطّى بالكامل** عندما لا يكون `GOOGLE_CLIENT_ID` مضبوطاً — وهو متغيّر اختياري مستقل عن `ADMIN_GOOGLE_EMAIL`. في نشرٍ فيه `ADMIN_GOOGLE_EMAIL` مضبوط و`GOOGLE_CLIENT_ID` فارغ، فإن أي ID token يصدره **أي** عميل OAuth تابع لجوجل لذلك البريد كان يمنح جلسة أدمن كاملة. هذا نمط token-confusion الكلاسيكي: التحقق من `email` دون التحقق من `aud`.

### التغيير
فشل مُغلق (fail closed) + مقارنة غير مشروطة:

```ts
const expectedClientId = process.env.GOOGLE_CLIENT_ID;
if (!expectedClientId) {
  console.error("[ADMIN] Google sign-in attempted but GOOGLE_CLIENT_ID is not configured — refusing.");
  return res.status(503).json({ error: "تسجيل الدخول عبر Google غير مهيّأ" });
}
if (payload.aud !== expectedClientId) {
  return res.status(401).json({ error: "توكن غير صالح" });
}
```

### لماذا الإصلاح آمن
- الدخول بكلمة مرور الأدمن (`POST /admin/login`) لم يُلمس — لا يوجد خطر فقدان الوصول.
- في أي نشر مُهيَّأ بشكل صحيح (`GOOGLE_CLIENT_ID` مضبوط) السلوك **مطابق تماماً** لما قبل التغيير.
- في النشر غير المُهيَّأ، السلوك السابق كان "قبول الجميع" — وهو ليس ميزة يمكن كسرها. 503 مع رسالة عربية واضحة أفضل من منح جلسة أدمن.

### اختبار الانحدار
`describe("C5 — Google admin sign-in must verify the token audience")` — 3 اختبارات: وجود المعالج، وجود حارس `if (!expectedClientId)` **وغياب** النمط المشروط `if (expectedClientId && payload.aud !== …)`، ومقارنة `payload.aud !== expectedClientId` غير مشروطة.

**طفرة:** إعادة الشرط المُقيَّد ⇒ يفشل اختبار fail-closed. ✅

---

## C2 — دفع زائد للتاجر في تسويات المتجر (Marketplace over-payment)

**الملفات:** `server/settlement.ts` (جديد: `vendorCommissionBase`)، `server/routes.ts` (موضع الاستدعاء)

### السبب الجذري
عند تسوية طلب، كانت قاعدة حساب مستحقّات التاجر:

```ts
const orderValue = (order as any).restaurantSubtotal ?? order.total ?? 0;
```

طلبات المطاعم تخزّن `restaurantSubtotal` صراحةً فتعمل بشكل صحيح. أما طلبات المتجر (`vendorProducts`) فلا تضبط هذا الحقل إطلاقاً — ذلك المسار يختم `vendorId`/`vendorName` فقط — فيسقط الحساب إلى `order.total`، وهو **يشمل `deliveryFee` و`serviceFee`** اللذين تحتفظ بهما OnWay. النتيجة: التاجر يُقيَّد له ما يقارب 90% من قيمة الرسوم (بعد خصم العمولة) في **كل طلب متجر**، ودفتر التسويات يخالف كشف الحساب الذي تعرضه لوحة التحكم.

### التغيير
دالة مشتركة مُصدَّرة تعكس نفس صيغة كشف حساب الأدمن:

```ts
export function vendorCommissionBase(order: {
  restaurantSubtotal?: number | null;
  total?: number | null;
  deliveryFee?: number | null;
  serviceFee?: number | null;
}): number {
  if (order?.restaurantSubtotal != null) return Math.max(0, Math.round(order.restaurantSubtotal));
  return Math.max(
    0,
    Math.round((order?.total || 0) - (order?.deliveryFee || 0) - (order?.serviceFee || 0)),
  );
}
```

وموضع الاستدعاء أصبح `const orderValue = vendorCommissionBase(order as any);`.

### لماذا الإصلاح آمن
- **سلوك طلبات المطاعم لم يتغيّر إطلاقاً** — الفرع الأول يعيد نفس `restaurantSubtotal` السابق.
- نسبة العمولة، وهيكل الدفتر، ومعرّفات التسوية (`${orderId}__${accountType}`) — كلها كما هي. لا تغيير في المخطّط ولا في مجموعات Firestore.
- `Math.max(0, …)` يمنع قاعدة سالبة لو تجاوزت الرسوم الإجمالي (طلب بخصم كبير).
- الآن الدفتر وكشف الحساب يستخدمان الصيغة نفسها ⇒ اختفى التعارض بينهما.

> **ملاحظة تشغيلية:** الإصلاح يمنع الدفع الزائد **من الآن فصاعداً**. التسويات التي قُيِّدت سابقاً بقاعدة خاطئة تبقى كما هي في الدفتر ولم تُلمس (تعديل بيانات مالية تاريخية خارج نطاق هذه المهمة). التصحيح — إن أُريد — يتم عبر `adminAdjustLedger` الموجود مسبقاً.

### اختبار الانحدار
`describe("C2 — vendor payout base must exclude delivery and service fees")` — 6 اختبارات وحدة حقيقية على الدالة المُصدَّرة (مطعم / متجر / متجر مع خصم / قاعدة غير سالبة / حقول مفقودة) + اختبار على موضع الاستدعاء يمنع عودة `restaurantSubtotal ?? order.total`.

**طفرتان:** إرجاع `total` من الدالة ⇒ تفشل 3 اختبارات. إعادة موضع الاستدعاء القديم ⇒ يفشل اختبار موضع الاستدعاء. ✅

---

## C3 — فجوة استرداد التسوية (فقدان مال نهائي)

**الملف:** `server/routes.ts` — `POST /api/driver/batch/complete-order`

### السبب الجذري
`getOrderById` تبتلع كل خطأ وتُرجع `null`. كان ترتيب العمليات:

1. مطالبة ذرّية بعَلَم `earningsCredited = true` (حارس عدم التكرار).
2. `const order = await getOrderById(orderId)`.
3. كل كتلة المال داخل `if (order) { … }`.

أي فشل عابر من Firestore في الخطوة 2 كان يعني: العَلَم **مُثبَّت** بينما كتلة المال كلها تُتخطّى. والأسوأ أن علامة `settlementPending` — التي تعتمد عليها دورة الاسترداد — كانت هي نفسها **داخل** تلك الكتلة، فلم يرَها كنس الاسترداد أبداً. وأي إعادة محاولة تُختصر فوراً إلى `alreadyCompleted`. النتيجة: مال ضائع نهائياً بلا أي أثر.

### التغيير
نُقلت قراءة الطلب إلى **ما قبل** المطالبة، مع رفض صريح:

```ts
const order = await getOrderById(orderId);
if (!order) {
  console.error(`[COMPLETE] order=${orderId} unreadable — refusing to claim earningsCredited`);
  return res.status(503).json({ error: "تعذّر قراءة الطلب، حاول مرة أخرى" });
}
```

### لماذا الإصلاح آمن
- لا نُطالب بعَلَم لا نستطيع التصرّف بناءً عليه. النافذة التي كانت تُنتج الخسارة أُزيلت بنيوياً، لا بمعالجة لاحقة.
- 503 قابل لإعادة المحاولة، وعميل السائق يعيد المحاولة؛ ولأن العَلَم لم يُثبَّت، فإعادة المحاولة تُكمل الطلب بشكل طبيعي.
- حارس عدم التكرار نفسه (المعاملة الذرّية) لم يتغيّر إطلاقاً — الحماية من الدفع المزدوج قائمة كما هي.

### اختبار الانحدار
اختباران داخل `describe("C3/C6 …")`: أن `getOrderById` يسبق `earningsCredited: true` في ترتيب الكود، وأن `if (!order)` يُتبع بـ `res.status(503)`.

**طفرة:** حذف حارس `if (!order)` ⇒ يفشل اختبار "unreadable order refuses to claim". ✅

---

## C6 — تعليق الطلب في حالة غير متّسقة (Order stranding)

**الملف:** `server/routes.ts` — `POST /api/driver/batch/complete-order`

### السبب الجذري
بعد تثبيت `earningsCredited`، كان كل ما يلي (تحديث الحالة، الإشعارات، التسويات، محاسبة الدفعة، القيد القديم) بلا `try/catch`. أي رمية استثناء بعد المطالبة كانت تُعلّق الطلب **إلى الأبد**: يبقى غير مُسلَّم، السائق لا يُقيَّد له شيء، الدفعة لا تُغلق (وهو ما يمنعه من استلام دفعات جديدة)، وكل إعادة محاولة تُختصر إلى `alreadyCompleted`. لا استرداد يدوي ولا آلي.

### التغيير
كل ما بعد المطالبة أُحيط بـ `try/catch`، مع تتبّع الخطوة الوحيدة **غير القابلة للتكرار**:

```ts
let legacyCreditApplied = false;
try {
  …
  await saveDriverCompletedOrder(phoneNumber, completedEntry);
  legacyCreditApplied = true;
  …
} catch (completionErr: any) {
  if (!legacyCreditApplied) {
    await orderRef.update({ earningsCredited: false }).catch(…);  // تحرير المطالبة لإعادة المحاولة
  } else {
    await orderRef.update({ settlementPending: true, settlementLastError: … }).catch(() => {});
  }
  throw completionErr;
}
```

### لماذا الإصلاح آمن
المفتاح هو التمييز بين ما يقبل التكرار وما لا يقبله:

- **قابل للتكرار:** كتابة الحالة، قيود التسوية (مفاتيحها `${orderId}__${accountType}` فتكرارها لا يفعل شيئاً)، محاسبة الدفعة، الإشعارات.
- **غير قابل للتكرار:** `saveDriverCompletedOrder` — تُلحق سجلاً بقائمة الطلبات المُنجزة للسائق.

لذلك: الفشل **قبل** تلك الخطوة ⇒ تحرير العَلَم آمن تماماً، وإعادة المحاولة تُعيد كل شيء بلا خطر قيد مزدوج. الفشل **بعدها** ⇒ العَلَم يبقى مثبَّتاً (لا قيد مزدوج) ويُعلَّم الطلب بـ `settlementPending` ليلتقطه كنس الاسترداد الموجود مسبقاً. في كلتا الحالتين يُعاد رمي الاستثناء فيصل الخطأ إلى العميل بدل ابتلاعه بصمت.

فشل التحرير نفسه (احتمال نادر جداً) يُسجَّل بمستوى `CRITICAL` مع رقم الطلب للتدخّل اليدوي.

### اختبار الانحدار
3 اختبارات: وجود `catch (completionErr`، ووجود فرع `if (!legacyCreditApplied) … earningsCredited: false`، وأن الفرع الآخر يضع `settlementPending: true` بدل التحرير.

**طفرة:** قلب الشرط إلى `if (legacyCreditApplied)` ⇒ يفشل اختبار التحرير. ✅

---

## التحقّق (Verification)

| البوابة | الأمر | النتيجة |
|---------|-------|---------|
| Typecheck | `npx tsc --noEmit` | ✅ نظيف (باستثناء خطأَي `expo-file-system` الموجودين قبل التغيير) |
| Unit tests | `npm run test:unit` | ✅ **71 ناجح / 0 فاشل** (13 مجموعة) — كانت 49 |
| Build | `npm run server:build` | ✅ `server_dist/index.js` — 517.1 kb |
| Lint | `npx expo lint` | ⚠️ انظر الملاحظة أدناه |
| Mutation | إعادة إدخال كل خلل | ✅ **7/7** طفرات تُكتشف |

### ملاحظة عن Lint
`npm run lint` يُخرج **18,554 مشكلة على مستوى المستودع بأكمله**، وهي حالة قائمة قبل هذا العمل ولا علاقة لها به (`server/routes.ts` وحده فيه 3,639 مشكلة على HEAD). فحص القواعد المُنتهَكة في الملفات التي عدّلتها يُظهر أن **كل** الزيادة من نوع `prettier/prettier` (تنسيق) — صفر قواعد صحّة جديدة:

| الملف | HEAD | بعد التغيير | الفارق | النوع |
|-------|------|-------------|--------|-------|
| `server/index.ts` | 236 | 247 | +11 | prettier فقط |
| `server/routes.ts` | 3,639 | 3,690 | +51 | prettier فقط |
| `server/settlement.ts` | 64 | 66 | +2 | prettier فقط |
| `tests/unit/critical-regressions.test.mjs` | — | **0** | — | مُنسَّق بالكامل |

تشغيل `prettier --write` على ملفات الخادم كان سيولّد فرقاً ضخماً غير متعلّق بالإصلاحات ويخالف قاعدة "أصغر رقعة ممكنة"، لذلك اقتصر التنسيق على الملف الجديد. **إصلاح التنسيق على مستوى المستودع بند مستقل خارج نطاق هذه المهمة.**

### اختبار الطفرات (تفصيل)

اختبارٌ لا ينقلب إلى أحمر عند إعادة إدخال الخلل ليس حارساً. أُعيد كل خلل على حدة وأُعيد تشغيل المجموعة:

| الطفرة | الاختبارات التي فشلت |
|--------|----------------------|
| C1 — إعادة fallback "سجل السائق موجود ⇒ أصدر توكن" | 1 |
| C4 — إزالة التهريب عن `m.text` | 1 |
| C5 — تقييد فحص `aud` بوجود `GOOGLE_CLIENT_ID` | 1 |
| C2 — إرجاع `total` كقاعدة الدفع | 3 |
| C2b — إعادة `restaurantSubtotal ?? order.total` بموضع الاستدعاء | 1 |
| C3 — السماح لطلب غير مقروء بتثبيت العَلَم | 1 |
| C6 — تحرير المطالبة بعد الخطوة غير القابلة للتكرار | 1 |

بعد الاسترجاع: 22/22 ناجح، والشجرة مطابقة للأصل.

---

## ما لم يُلمس (كما طُلب)

- **26 بنداً High** — لم يُعدَّل أي منها.
- **19 بنداً Medium** — لم يُعدَّل أي منها.
- **6 بنود Low** — لم تُلمس.
- لا إعادة هيكلة، لا تحسين أداء، لا Kaizen.
- **لا كسر توافق:** كل المسارات والاستجابات ومخطّط قاعدة البيانات ومجموعات Firestore كما هي. المسارات الوحيدة التي تغيّر سلوكها هي مسارات الفشل التي كانت خاطئة أصلاً (401 بدل توكن مسروق، 503 بدل خسارة مال، 503 بدل جلسة أدمن مجانية).

---

## مخاطر متبقّية

1. **لا تحقّق تشغيلي حيّ.** لا تتوفّر في هذه البيئة `FIREBASE_SERVICE_ACCOUNT` ولا أي بيانات اعتماد إنتاج، فلم يُشغَّل أي من هذه المسارات مقابل Firestore حقيقي. التحقّق منطقي + اختبارات وحدة + اختبار طفرات. **يجب اختبار التدفّقات التالية يدوياً على Replit/VPS قبل الإطلاق:**
   - دخول سائق مسجَّل بتوكن عميل صالح ⇒ ينجح؛ وبرقم سائق آخر ⇒ 401.
   - فتح محادثة دعم تحتوي `<img src=x onerror=alert(1)>` في لوحة التحكم ⇒ يُعرض كنص.
   - إكمال طلب متجر ⇒ مقارنة المبلغ المُقيَّد في الدفتر بكشف حساب التاجر (يجب أن يتطابقا الآن).
   - إكمال الطلب نفسه مرتين ⇒ لا قيد مزدوج.

2. **`GOOGLE_CLIENT_ID` صار إلزامياً لدخول Google.** أي نشر يعتمد اليوم على دخول Google دون ضبط هذا المتغيّر سيتلقّى 503. هذا مقصود (فشل مُغلق)، لكنه يستدعي التأكد من ضبط المتغيّر في بيئة الإنتاج قبل النشر. دخول كلمة المرور يبقى بديلاً متاحاً.

3. **التسويات التاريخية الخاطئة (C2) لم تُصحَّح.** الطلبات التي سُوِّيت قبل الإصلاح تحمل قاعدة دفع مبالغاً فيها. تصحيحها قرار مالي للمالك عبر `adminAdjustLedger`، وليس جزءاً من إغلاق الخلل.

4. **اختبارات C1/C3/C4/C5/C6 تفحص نصّ المصدر، لا السلوك في زمن التشغيل.** السبب أن `routes.ts` لا يُصدِّر إلا `registerRoutes`، و`admin.html` قالب نصي، ولا توجد بيانات اعتماد لتشغيل خادم حقيقي في CI. كل تأكيد يستهدف البنية الخاطئة تحديداً، وقد أثبت اختبار الطفرات أنه يلتقطها — لكن إعادة كتابة الخلل **بصياغة مختلفة جوهرياً** قد تفلت. الحارس الأمتن يتطلّب تفكيك `routes.ts` لتصبح المعالجات قابلة للاستيراد، وهو بند High خارج النطاق. (`vendorCommissionBase` في C2 هو الوحيد الذي يُختبر سلوكياً بالكامل لأنه مُصدَّر فعلاً.)

5. **تشغيل PM2 بنسخة واحدة (`instances: 1`) ما زال شرطاً معمارياً** — طابور السائقين وجلسات الأدمن ومحدّد المعدّل كلها في ذاكرة العملية. لم يتغيّر هذا، وهو موثَّق ضمن بنود High.
