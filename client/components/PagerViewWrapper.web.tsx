import React from "react";
import { View } from "react-native";

const PagerViewWeb = React.forwardRef((_props: any, _ref: any) => {
  return <View />;
});

PagerViewWeb.displayName = "PagerViewWeb";

export default PagerViewWeb;
